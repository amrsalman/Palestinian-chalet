import 'package:flutter/material.dart';

class Favouret extends StatelessWidget {
  const Favouret({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

backgroundColor: Colors.purple,
body: Center(
  child: Text(
    'Favouret',
    style: TextStyle(
        color: Colors.white,
      fontSize: 60,
      fontWeight: FontWeight.bold,

    ),
  ),
),









    );
  }
}
